# 🔥 Руководство по установке Base Void Linux

# Прежде чем начать

В этом руководстве описывается **ручная установка Void Linux** с использованием прямого разделения диска, chroot и явной настройки системы.
Это **не автоматический установщик**.

## ⚠️ Читайте внимательно

- Это руководство **предполагает знакомство с Linux**, терминалом и основными понятиями системы (диски, разделы, загрузка, службы).
- Некоторые команды **безвозвратно удаляют данные** (`parted`, `mkfs`, `umount -R`).
- Ошибка при определении диска (`/dev/sdX`, `/dev/nvmeX`) может привести к **полной потере данных**.
- Прочитайте **полное руководство перед выполнением любой команды**.

## 🖥️ Рекомендуемая среда

- **VM (VirtualBox, QEMU, KVM и т. д.)** для тестирования и обучения.
- Выделенное оборудование **нет важных данных**.
- Лабораторная среда или сознательный объект.

❌ **Не рекомендуется** для непосредственного использования в производстве без доработок.

## 🔐 О безопасности

В процессе установки некоторые настройки **отдают предпочтение практичности**, а не безопасности:
- Вход пользователя root через SSH может быть временно включен.
- Аутентификация по паролю может быть активна.
- Может быть разрешена совместимость с устаревшими версиями (например, `ssh-rsa`).

👉 **Эти настройки необходимо просмотреть после установки**, особенно на системах, подключенных к сети.

## 🧠 Важно знать

- Выполняйте команды **одну за**, проверяя вывод.
- Настройте имена дисков, сетевые интерфейсы и пользователей в соответствии с вашей системой.
- **Не копируйте и вставляйте вслепую**.
– Если у вас есть сомнения, **остановитесь** и просмотрите текущий шаг.

## 🛠️ В случае ошибки

Если что-то пойдет не так:
- Не перезагружайтесь вслепую.
- Соберите перегородки.
- Войдите в систему с помощью chroot.
- Проверьте GRUB, EFI и initramfs.

Ошибки – это часть этого. Понимание ошибки — это то, что отличает пользователя от оператора.

---

> Это руководство предназначено для пользователей, которые предпочитают **полный** контроль** над установкой, следуя классическому подходу Unix:
> **понять → настроить → проверить → продолжить**.

## Начать установку
Начните с ISO-образа Void Linux (x86_64 glibc или musl).

1. Войдите в систему как root
```bash
логин: root
пароль: voidlinux
```
2. Переключите оболочку с *sh* на *bash*.
*dash/sh* **НЕ реализует** некоторые функции, используемые многими скриптами.
```bash
бить
```
3. Измените раскладку клавиатуры на **ABNT2**, обеспечив правильное отображение диакритических знаков и символов:
```bash
клавиши загрузки br-abnt2
```

4. Подключитесь к Интернету
- Для **Wi-Fi** *(если подключено по кабелю, пропустите этот шаг)*:
```bash
wpa_passphrase "WIFI_NETWORK_NAME" "NETWORK_PASSWORD" > wifi.conf
wpa_supplicant -B -i wlan0 -c wifi.conf
dhcpcd wlan0
```
> 📌 **Примечание:** `wlan0` может отличаться (`wlp2s0`, `wlp0s3` и т. д.).
> Используйте команду ниже, чтобы определить правильный интерфейс:
>
> ``` баш
> ip -br а
> ```

5. Проверьте соединение:
```bash
пинг -c3 8.8.8.8
ping -c3 repo-default.voidlinux.org
```

## Включите вход пользователя **root** через SSH (необязательно).
Этот шаг применим только в том случае, если система работает на виртуальной машине; в случае локальной загрузки (без виртуальной машины) установка может проходить нормально через локальный терминал.
- Это необходимо для доступа к **ВМ с хоста** и продолжения установки удаленно; после этого команды можно вставлять/выполнять непосредственно в терминале через SSH.

1. Настройте SSH
```bash
echo 'PermitRootLogin да' >> /etc/ssh/sshd_config
```
2. Перезапустите службу ssh.
```bash
св перезапустить sshd
```

3. Просмотр IP-адреса сетевого интерфейса.
```bash
маршрут ip -4 получить 1.1.1.1 | awk '{print $7}'
```
>Запишите IP-адрес сетевого интерфейса и используйте его для подключения к виртуальной машине через SSH.

4. Получите доступ к виртуальной машине через SSH с хоста.
```bash
sudo ssh <ip-da-vm>
```
> Пароль по умолчанию: `voidlinux`

## Настройте цветную подсказку в терминале (необязательно)
Он отобразит пользователя, хост, текущий путь и статус последней команды:
```bash
экспорт PROMPT_COMMAND='RET=$?'
экспорт PS1='\[\e[1;33м\]\u\[\e[0м\]@\[\e[1;35м\]\h\[\e[0м\]:\[\e[0;37м\]\w\[\e[0м\] \[\e[1;32м\]$( [ $RET -eq 0 ] && printf ✔ || printf "\e[1;31m✘$RET" )\[\e[0m\] \$ '
```
> 📌 Эта подсказка действительна только для текущего сеанса; чтобы сделать его постоянным, добавьте его в `.bashrc`.

## Установите необходимые пакеты
⚠️ **ВАЖНО:**
```bash
xbps-install -Sy xbps parted nano vim zstd xz bash-completion
```

## Разбить диск на разделы
1. Определите диск
```bash
fдиск -л | grep -E '^(Диск|Диск) '
```
> Для учебника мы предположим, что `/dev/sda`

2. Настройте приведенные ниже переменные в соответствии с диском, который будет использоваться (**ВАЖНО**):
```bash
# Диски SATA/SCSI (sdX)
экспортировать УСТРОЙСТВО=/dev/sda
экспортировать DEV_EFI=${DEVICE}2
экспортировать DEV_ROOT=${DEVICE}3
```

> 📌 **Примечание:**
> Для дисков **NVMe** меняется суффикс раздела (`p`):
> ``` баш
> экспортировать УСТРОЙСТВО=/dev/nvme0n1
> экспортировать DEV_EFI=${DEVICE}p2
> экспортировать DEV_RAIZ=${DEVICE}p3
> ```

3. Разбейте диск на разделы с помощью **parted** (автоматический режим).
Эта схема создает:
- раздел BIOS (bios_grub)
- Раздел EFI (ESP)
- Корневой раздел (ROOT)
```bash
Wipefs -a "${DEVICE}"
parted --script "${DEVICE}" -- \
mklabel gpt \
mkpart основной 1 МБ 2 МБ имя 1 набор BIOS 1 bios_grub на \
mkpart основной fat32 2 МБ 514 МБ имя 2 набора EFI 2 esp на \
mkpart основной 514МБ 100% имя 3 КОРЕНЬ \
выровнять-проверить оптимальность 1 \
выровнять-проверить оптимальное 2 \
выравнивание-проверка оптимального 3
parted --script "${DEVICE}" -- распечатать
```

## Форматирование разделов
```bash
# Форматируем корневой раздел (ext4)
mkfs.ext4 -F ${DEV_RAIZ}

# Отформатируйте раздел EFI (FAT32)
mkfs.fat -F32 -I ${DEV_EFI}
```

## Смонтируем тома в `/mnt`
```bash
# Монтируем корневой раздел
смонтировать ${DEV_RAIZ} /mnt

# Создаем необходимые точки монтирования
/t /mnt/{hame,boot/efi,var/log,var/cache, proced, proc, Proc,)

# Монтируем раздел EFI
смонтировать ${DEV_EFI} /mnt/boot/efi
```

## Установите базовую систему
Устанавливает базовую систему Void Linux в смонтированную среду `/mnt`, включая ядро, прошивку, загрузчик, сеть и необходимые инструменты.
```bash
xbps-install -Sy -R https://repo-default.voidlinux.org/current \
-р /мнт\
базовая система e2fsprogs grub-x86_64-efi dracut Linux \
linux-headers linux-firmware linux-firmware-network glibc-locales \
xtools dhcpcd openssh vim nano grc zstd xz bash-completion vpm vsv \
wget net-tools socklog-void tmate ncurses chrony
```

> 📌 **Примечание:**
> - `grub-x86_64-efi` → загрузчик UEFI
> - `linux` → ядро
> - `linux-firmware-network` → сетевые драйверы
> - `xtools` → необходимо обязательно использовать `xgenfstab`

## Создать `fstab`
Автоматически генерирует файл постоянного монтирования системы.
```bash
xgenfstab -U /mnt > /mnt/etc/fstab
```

## Вход и система (chroot)
Получите доступ к системе, установленной в `/mnt`, чтобы продолжить настройку.
```bash
xchroot/mnt/bin/bash
```

## Генерируем INITRAMFS
Конфигурация Dracut для сред виртуализации (безопасная для виртуальных машин)
```bash
cat > /etc/dracut.conf.d/99-vm-safe.conf << 'EOF'
# /etc/dracut.conf.d/99-vm-safe.conf
только хост = нет
сжать="gzip"
add_drivers+=" virtio virtio_pci virtio_blk virtio_net virtio_scsi "
ЭОФ
```

Автоматически определяет установленную версию ядра и генерирует соответствующий `initramfs` с помощью **dracut**.
```bash
моды=(/usr/lib/modules/*)
KVER=$(базовое имя "${mods[0]}")
эхо ${KVER}
dracut --force --kver ${KVER}
```

## Настройте GRUB

> 📌Оба метода (BIOS и UEFI) установлены специально.
> Это позволяет одному и тому же диску загружаться как в системах **Legacy BIOS**, так и в **UEFI**, что увеличивает переносимость между компьютерами.

1. Создайте каталог поддержки GRUB:
```bash
mkdir -p /boot/grub
```

2. Установите GRUB для **BIOS (Legacy)**:
```bash
grub-install --target=i386-pc ${DEVICE}
```

3. Установите GRUB для **UEFI**:
```bash
grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=void
```

4. Создайте резервную версию UEFI (универсальную загрузку).
Этот файл гарантирует загрузку, даже если NVRAM стерта:
```bash
mkdir -p /boot/efi/EFI/BOOT
cp -f /boot/efi/EFI/void/grubx64.efi /boot/efi/EFI/BOOT/BOOTX64.EFI
```

5. Сгенерируйте окончательный файл конфигурации GRUB:
```bash
grub-mkconfig -o /boot/grub/grub.cfg
```

## Создание и настройка пользователей

⚠️ **ВАЖНО:** ниже укажите настоящее имя пользователя.
```bash
экспорт NEWUSER=ваш_пользователь_здесь
```

Создайте пользователя с домашним каталогом, базовыми группами и оболочкой Bash:
```bash
useradd -m -G audio,video,wheel,tty -s /bin/bash ${NEWUSER}
```

Установите пароль пользователя (***ВАЖНО***)
```bash
пароль ${NEWUSER}
```

Установите пароль пользователя root (***ВАЖНО***)
```bash
корневой пароль
```

Измените оболочку по умолчанию пользователя root на Bash
```bash
chsh -s /bin/bash корень
```

## Основные настройки
```bash
# Установить имя хоста
echo void > /etc/имя хоста

# Установить местное время
ln -sf /usr/share/zoneinfo/America/Sao_Paulo /etc/localtime

# Сетар Локальный
sed -i 's/#en_US.UTF-8/en_US.UTF-8/' /etc/default/libc-locales
sed -i 's/#pt_BR.UTF-8/pt_BR.UTF-8/' /etc/default/libc-locales

# Генерируем локали:
xbps-reconfigure -f glibc-locales

# Исправьте возможную ошибку в символической ссылке /var/service (важно):
rm -f /вар/сервис
ln -sf /etc/runit/runsvdir/default /var/service

# Активируйте некоторые услуги:
ln -sf /etc/sv/dbus /var/service/
ln -sf /etc/sv/dhcpcd /var/service/
ln -sf /etc/sv/sshd /var/service/
ln -sf /etc/sv/nanoklogd /var/service/
ln -sf /etc/sv/socklog-unix /var/service/
ln -sf /etc/sv/chronyd /var/service/

# загрузите собственный svlogtail (необязательно, но рекомендуется):
wget --quiet --no-check-certificate -O /usr/bin/svlogtail \
"https://raw.githubusercontent.com/voidlinux-br/void-install/refs/heads/main/svlogtail" && \
chmod +x /usr/bin/svlogtail

# Создайте resolv.conf
printf 'сервер имен 1.1.1.1\nсервер имен 8.8.8.8\n' > /etc/resolv.conf

#Настроить sudo — группу колес (необязательно, но рекомендуется)
cat << 'EOF' > /etc/sudoers.d/g_wheel
%wheel ALL=(ALL:ALL) NOPASSWD: ALL
ЭОФ

#Необходимые разрешения
chmod 440 /etc/sudoers.d/g_wheel
```

## Настраиваем `/etc/xbps.d/00-repository-main.conf`
*(Необязательно, но рекомендуется)*

Создает каталог конфигурации **XBPS** (если он еще не существует) и определяет список официальных и альтернативных репозиториев.
**репо-быстрые** репозитории, как правило, имеют лучшую задержку.

```bash
mkdir -pv /etc/xbps.d

cat << 'EOF' > /etc/xbps.d/00-repository-main.conf
# Официальный репозиторий (быстро – лучшая задержка)
репозиторий = https://repo-fastly.voidlinux.org/current
#repository=https://repo-fastly.voidlinux.org/current/nonfree
#repository=https://repo-fastly.voidlinux.org/current/multilib
#repository=https://repo-fastly.voidlinux.org/current/multilib/nonfree

# Альтернативный репозиторий (Chili Linux)
репозиторий = https://void.chililinux.com/voidlinux/current
#repository=https://void.chililinux.com/voidlinux/current/extras
#repository=https://void.chililinux.com/voidlinux/current/nonfree
#repository=https://void.chililinux.com/voidlinux/current/multilib
#repository=https://void.chililinux.com/voidlinux/current/multilib/nonfree
ЭОФ
```

## Настраиваем `/etc/rc.conf`
Устанавливает часовой пояс консоли по умолчанию, раскладку клавиатуры и шрифт.
Меняйте по мере необходимости.
```bash
кот << 'EOF' > /etc/rc.conf
TIMEZONE=Америка/Сан-Паулу
KEYMAP=br-abnt2
FONT=Lat2-Terminus16
ЭОФ
```

Модули Virtio (виртуальная машина).
```bash
cat > /etc/modules-load.d/virtio.conf << 'EOF'
виртио
virtio_pci
virtio_net
virtio_blk
virtio_scsi
ЭОФ
```

## Настраиваем пользователя `.bashrc`
Создает файл .bash_profile по умолчанию и обеспечивает автоматическую загрузку .bashrc при входе в систему.
> ⚠️ Убедитесь, что пользователь был создан на предыдущем шаге.

```bash
# Загрузите .bashrc по умолчанию в /etc/skel
wget --quiet --no-check-certificate \
-O /etc/skel/.bashrc \
"https://raw.githubusercontent.com/voidlinux-br/void-install/refs/heads/main/.bashrc"

корень chown: root /etc/skel/.bashrc
chmod 644 /etc/skel/.bashrc

# Создать файл .bash_profile по умолчанию
cat << 'EOF' > /etc/skel/.bash_profile
# ~/.bash_profile — загружает .bashrc в Void

# Если .bashrc существует, загрузите
если [ -f ~/.bashrc ]; затем
источник ~/.bashrc
быть
ЭОФ

# Копируем под root и пользователя
для d в /root "/home/${NEWUSER}"; делать
cp -f /etc/skel/.bash_profile "$d/"
cp -f /etc/skel/.bashrc "$d/"
сделанный

# Настройте права пользователя
chown "${NEWUSER}:${NEWUSER}" \
"/home/${NEWUSER}/.bash_profile" \
"/home/${NEWUSER}/.bashrc"

chmod 644 \
"/home/${NEWUSER}/.bash_profile" \
"/home/${NEWUSER}/.bashrc"
```

## Настройте SSH
*(Необязательно, но рекомендуется)*

Создает дополнительный файл конфигурации для **sshd**, оставляя основной файл нетронутым.
```bash
mkdir -pv /etc/ssh/sshd_config.d

cat << 'EOF' > /etc/ssh/sshd_config.d/10-custom.conf
# Общие настройки
РазрешениеTTY да
ПринтМотд да
PrintLastLog да
Баннер /etc/issue.net

# Аутентификация
РазрешитьRootLogin да
Аутентификация по паролю да
KbdInteractiveAuthentication да
ВызовОтветАутентификация да
PubkeyAuthentication да
PubkeyAcceptedKeyTypes=+ssh-rsa
AuthorizedKeysFile .ssh/authorized_keys
ИспользоватьPAM да

# Функции
X11Переадресация да
Подсистема sftp внутренний-sftp
ЭОФ
```
> ⚠️ Рекомендуется просмотреть и усилить эти настройки SSH после первой загрузки, особенно в системах, подключенных к Интернету.

## Завершить установку
Сайр делает chroot
```bash
Выход
```
Отмонтируйте все разделы, смонтированные в `/mnt` (включая субтома и `/boot/efi`):
```bash
размонтировать -R /mnt
```
Перезагрузите физическую машину или виртуальную машину, чтобы проверить фактическую загрузку:
```bash
перезагрузить
```
> 📌 **Примечание: не забудьте удалить установочный носитель.

# 🎉 Наслаждайтесь!
**Void Linux** установлен и готов к использованию.

# ОТКАЗ ОТ ОТВЕТСТВЕННОСТИ

> Это руководство бесплатное: вы можете использовать, копировать, изменять и распространять по своему усмотрению.
> Содержимое предоставляется по **Лицензии MIT** и может включать в себя отрывки или команды, полученные из программного обеспечения с открытым исходным кодом, в соответствии с их собственными лицензиями.
>
> Никаких гарантий — здесь все поставляется **как есть**.
> Используйте на свой страх и риск. Ни автор, ни участники, ни Void Linux не несут ответственности за утрату, повреждение, системные сбои или любые последствия использования этого материала.
>
> Вы можете просмотреть, адаптировать и создать свою собственную версию этого руководства.

