# 🧩 ОБУЧЕНИЕ ПО VOID LINUX — ГИБРИДНАЯ УСТАНОВКА (UEFI + BIOS) С EXT4, XFS, JFS ИЛИ BTRFS (СУБТОМЫ), LUKS, HiberNation И ZRAM
### ПЕРЕСМОТРЕННАЯ И ПОДТВЕРЖДЕННАЯ ВЕРСИЯ — ПРАВИЛЬНОЕ РАЗДЕЛЕНИЕ + УНИВЕРСАЛЬНАЯ ЗАГРУЗКА

Это руководство устанавливает полностью **гибридную** Void Linux, способную загружать компьютер любого типа — старый, новый или проблемный:

- 💾 **Современный UEFI** (с обычным вводом и резервным режимом)
- 🧮 **BIOS/Legacy** (полная совместимость)
- 🧰 **GPT с BIOS Boot (EF02)** — максимальная поддержка старого оборудования
- 🚀 **Btrfs с субобъемами** (опционально), готовые снимки
- 🔐 **LUKS1 полностью совместим с GRUB**
- 🌙 **Настоящая гибернация через файл подкачки**
- 🧊 **ZRAM настроен на повышенную производительность**
- 🧱 **Полная поддержка EXT4, XFS, JFS и BTRFS**
- 💡 **Initramfs/GRUB настраивается автоматически (LUKS + резюме)**

📌 **Без компромиссов, без переустановки GRUB, без траты времени.**
📌 **Гарантированная загрузка даже на машине со стертой NVRAM (резервный вариант BOOTX64.EFI).**

---

# ▶️ 1. Загрузка или Live ISO

Предложение: используйте версию glibc для превосходной совместимости:
- скачать iso с:
```
https://repo-default.voidlinux.org/live/current/void-live-x86_64-20250202-base.iso
```
- или найдите последнюю версию по адресу:
```
https://voidlinux.org/download/
```

1. Войдите в систему как root.
```bash
логин: root
пароль: voidlinux
```

2. Переключите оболочку с *sh* на *bash*.
*dash/sh* **НЕ реализует** некоторые функции, используемые многими скриптами.
```bash
бить
```

3. Измените раскладку клавиатуры на **ABNT2**, обеспечив правильное отображение диакритических знаков и символов:
```bash
клавиши загрузки br-abnt2
```

4. Вставьте в терминал (необязательно) — подсказка с цветами, user@host:path и статусом последней команды (✔/✘). Полезно и красиво.
```bash
экспорт PS1='\[\e[1;32м\]\u\[\e[1;33м\]@\[\e[1;36м\]\h\[\e[1;31м\]:\w \
$([[ $? -eq 0 ]] && echo -e "\e[1;32m✔" || echo -e "\e[1;31m✘$?") \
\[\e[0m\]\$ '
```

# ▶️ 2. Подключитесь к Интернету
- Для **Wi-Fi** *(если подключено по кабелю, пропустите этот шаг)*:
```bash
wpa_passphrase «SSID» «ПАРОЛЬ» > wifi.conf
wpa_supplicant -B -i wlan0 -c wifi.conf
dhcpcd wlan0
```

1. Проверьте соединение:
```bash
пинг -c3 8.8.8.8
ping -c3 repo-default.voidlinux.org
```

2. Установите необходимые пакеты:
⚠️ **ВАЖНО:**
```bash
xbps-install -Sy xbps parted jfsutils xfsprogs nano zstd xz bash-completion
```
---

# ▶️ 3. Определить диск
1. Перечислите доступные диски и запишите имя устройства (например: `/dev/sda`, `/dev/vda`, `/dev/nvme0n1`):
```bash
fдиск -л | grep -E '^(Диск|Диск) '
```

# ▶️ 4. Определите переменные, используемые в уроке:
⚠️ **ВАЖНО:**

1. Определите устройства **ПЕРЕД** использованием:
> 1. **Мы предполагаем** для руководства `/dev/sda` (обычный) или `/dev/nvme0n1` (nvme)
> 2. **Настройте** в соответствии с вашим диском (выберите только **одну** или **другую** модель)

Для **обычных** дисков (например, /dev/sda)
```bash
экспортировать УСТРОЙСТВО=/dev/sda
экспортировать DEV_BIOS=${DEVICE}1
экспортировать DEV_EFI=${DEVICE}2
экспортировать DEV_ROOT=${DEVICE}3
экспортировать DEV_LUKS=/dev/mapper/cryptroot
```
Для дисков **NVMe** (например, /dev/nvme0n1) суффикс раздела меняется (`p`).
```bash
экспортировать УСТРОЙСТВО=/dev/nvme0n1
экспортировать DEV_BIOS=${DEVICE}p1
экспортировать DEV_EFI=${DEVICE}p2
экспортировать DEV_RAIZ=${DEVICE}p3
экспортировать DEV_LUKS=/dev/mapper/cryptroot
```

> 📌 **Примечание:**
> УСТРОЙСТВО → весь диск
DEV_BIOS → загрузочный раздел BIOS (1–2 МБ, нет ФС, не монтируется)
DEV_EFI → Раздел EFI (FAT32)
DEV_ROOT → корневой раздел (обычный или LUKS)
DEV_LUKS → Сопоставление LUKS (/dev/mapper/cryptroot)

- 👉Здесь вы определяете анатомию диска. Все остальное в руководстве просто следует этим переменным.
- 🔎Зачем это нужно?
Потому что объявление всего в начале делает следующий процесс защищенным от опечаток.

2. Определите **KEYMAP** и **TIMEZONE** (измените при необходимости):
```bash
экспортировать KEYMAP=br-abnt2
```
```bash
экспортировать TIMEZONE=Америка/Сан-Паулу
```

---

# ▶️ 5. Разбиение диска
> - Раздел BIOS **ДОЛЖЕН** быть первым. Это повышает совместимость со старыми материнскими платами, проблемными загрузчиками и BIOS, которые ожидают загрузочный код в первых областях диска.
> - ESP может прийти позже без каких-либо проблем — UEFI не заботится об этой позиции.

### Идеальный и правильный порядок:

- 1️⃣ Загрузка BIOS (EF02)
- 2️⃣ ESP (система EFI, FAT32)
- 3️⃣ Btrfs/Ext4/Xfs/Jfs (корневой)

### Разделение с использованием parted (автоматически)
> Здесь **DEVICE** уже определен, поэтому «волшебной» переменной нет.
```
Wipefs -a "${DEVICE}"
parted --script "${DEVICE}" -- \
mklabel gpt \
mkpart основной 1 МБ 2 МБ имя 1 набор BIOS 1 bios_grub на \
mkpart основной fat32 2 МБ 514 МБ имя 2 набора EFI 2 esp на \
mkpart основной 514МБ 100% имя 3 КОРЕНЬ \
выровнять-проверить оптимальность 1 \
выровнять-проверить оптимальное 2 \
выравнивание-проверка оптимального 1
parted --script "${DEVICE}" -- распечатать
```
> - Раздел 1 → загрузка BIOS (bios_grub, нет ФС, не монтируется)
> - Раздел 2 → EFI (FAT32)
> - Раздел 3 → ROOT (позже отформатируем его с помощью EXT4/XFS/JFS/BTRFS, с LUKS или без него)
> - Я использовал mkpart Primary 514MiB 100% без точного указания ФС, чтобы не связывать ФС. Вы выберете FS позже.
---

# ▶️ 6. Выберите режим установки (НОРМАЛЬНЫЙ или LUKS)
⚠️ **ВАЖНО:**
> Выберите ТОЛЬКО ОДИН из двух блоков ниже.
**НЕ** для выполнения обоих шагов.

1. НОРМАЛЬНАЯ УСТАНОВКА **(без LUKS)**
```bash
экспортировать ДИСК="${DEV_RAIZ}"
```
- Устанавливает DISK на фактическое устройство /dev/sda3.

2. УСТАНОВКА **С ПОМОЩЬЮ LUKS** (зашифрованный корень)
```
# Шифровать ТОЛЬКО корневой раздел на LUKS1 (совместимый с GRUB) - никогда весь диск
# Зашифруйте раздел, подтвердив YES:
cryptsetup luksFormat --type luks1 "${DEV_RAIZ}"

# Откройте раздел, используя свою парольную фразу.
cryptsetup открыть cryptroot "${DEV_RAIZ}"

# С этого момента настоящим корнем является подключенное устройство
экспортировать ДИСК="${DEV_LUKS}"
```
- LUKS находится поверх /dev/sda3, а не всего диска.
- Система будет установлена в /dev/mapper/cryptroot.

👉 С этого момента ВСЕ использует $DISK..

---

# ▶️ 7. Создайте файловую систему (ФС) и смонтируйте корень
⚠️ **ВАЖНО:**
> Выберите ТОЛЬКО ОДИН из двух блоков ниже.

1. **EXT4**
```
mkfs.ext4 -F "${DISK}" -L КОРЕНЬ
mount -v "${DISK}" /mnt
```
2. **XFS**
```
mkfs.xfs -f "${DISK}"
mount -v "${DISK}" /mnt
```
3. **JFS**
```
mkfs.jfs -f "${DISK}"
mount -v "${DISK}" /mnt
```
4. **Простой BTRFS**
```
mkfs.btrfs -f "${DISK}" -L КОРЕНЬ
mount -v "${DISK}" /mnt
```
5. **BTRFS с подтомами**
```
mkfs.btrfs -f "${DISK}" -L КОРЕНЬ

смонтировать ${DISK} /mnt
btrfs субтом создать /mnt/@
btrfs subvolume создать /mnt/@home
btrfs субтом создать /mnt/@log
btrfs subvolume создать /mnt/@cache
btrfs subvolume создать /mnt/@snapshots
размонтировать /mnt

mount -o defaults,noatime,ssd,compress=zstd:3,discard=async,space_cache=v2,commit=300,subvol=/@ ${DISK} /mnt
mkdir -p /mnt/{boot/efi,home,var/log,var/cache,.snapshots,swap}

mount -o defaults,noatime,ssd,compress=zstd:3,discard=async,space_cache=v2,commit=300,subvol=/@home ${DISK} /mnt/home
mount -o defaults,noatime,ssd,compress=zstd:3,discard=async,space_cache=v2,commit=300,subvol=/@cache ${DISK} /mnt/var/cache
mount -o defaults,noatime,ssd,compress=zstd:3,discard=async,space_cache=v2,commit=300,subvol=/@log ${DISK} /mnt/var/log
mount -o defaults,noatime,ssd,compress=zstd:3,discard=async,space_cache=v2,commit=300,subvol=/@snapshots ${DISK} /mnt/.snapshots
```
---

# ▶️ 8. Подготовьте и соберите ESP (EFI)
```
mkfs.fat -F32 -I "${DEV_EFI}"
mkdir -p /mnt/boot/efi
mount -v "${DEV_EFI}" /mnt/boot/efi
```
>💡Раздел биоса (${DEV_BIOS}) не имеет файловой системы, не форматируется, не монтируется.
---

# ▶️ 9. Установка или Void Linux без chroot

1. Скопируйте ключи репозитория (ключи XBPS), которые будут использоваться в chroot (/mnt).
```
mkdir -p /A{tc, Vapor/xbps/xbps)
cp -rpaf /var/db/xbps/keys/*.plist /mnt/var/db/xbps/keys/
cp -fpa /etc/resolv.conf /mnt/etc/resolv.conf
```

2. Установите базовую систему на вновь смонтированный диск:
```
xbps-install -Sy -R https://repo-default.voidlinux.org/current \
-р /мнт\
base-system btrfs-progs cryptsetup grub grub-x86_64-efi dracut linux \
linux-headers linux-firmware linux-firmware-network glibc-locales \
xtools dhcpcd openssh vim nano grc zstd xz bash-completion vpm vsv \
socklog-void wget net-tools tmate ncurses jfsutils xfsprogs duf дерево eza chrony
```
---

# ▶️ 10. Герар fstab no /mnt (chroot)
```bash
# Создать fstab в /mnt/etc/fstab
xgenfstab -U /mnt > /mnt/etc/fstab
```

```bash
#проверяем, правильно ли оно было сгенерировано
кот /mnt/etc/fstab
```

# ▶️ 11. Доступ к установленной системе с помощью chroot

1. Не используйте:
```
xchroot/mnt/bin/bash
```
---

# ▶️ 12. Начальные настройки (в chroot)
```
# настроить имя хоста — определяет имя хоста
echo void > /etc/имя хоста

# настроить часовой пояс - определяет часовой пояс
ln -sfv /usr/share/zoneinfo/"${TIMEZONE}" /etc/localtime

# настраиваем локали
-i -e 's/^#\(en_.Utf-8 UTF-8\)/)/' \
-E 's/^#\pt_br.br.utf-8 UTF-8\)/' \' \
/etc/default/libc-locales

# генерировать локали
xbps-reconfigure -f glibc-locales

# Исправьте возможную ошибку в символической ссылке /var/service (важно):
rm -f /вар/сервис
ln -sf /etc/runit/runsvdir/default /var/service

# Активировать некоторые услуги
ln -sf /etc/sv/dbus /var/service/
ln -sf /etc/sv/dhcpcd /var/service/
ln -sf /etc/sv/sshd /var/service/
ln -sf /etc/sv/nanoklogd /var/service/
ln -sf /etc/sv/socklog-unix /var/service/
ln -sf /etc/sv/chronyd /var/service/

# Настройте sudo - группу колес (необязательно, но рекомендуется)
cat << 'EOF' > /etc/sudoers.d/g_wheel
%wheel ALL=(ALL:ALL) NOPASSWD: ALL
ЭОФ
#Необходимые разрешения
chmod 440 /etc/sudoers.d/g_wheel
```

## Создание и настройка пользователей

⚠️ **ВАЖНО:** ниже укажите настоящее имя пользователя.
```bash
экспорт NEWUSER=ваш_пользователь_здесь
```

Создайте пользователя с домашним каталогом, базовыми группами и оболочкой Bash:
```bash
useradd -m -G audio,video,wheel,tty -s /bin/bash ${NEWUSER}
```

Установите пароль пользователя (***ВАЖНО***)
```bash
пароль ${NEWUSER}
```

Установите пароль пользователя root (***ВАЖНО***)
```bash
корневой пароль
```

Измените оболочку по умолчанию пользователя root на Bash
```bash
chsh -s /bin/bash корень
```
---

# ▶️ 13. Настройте UUID
⚠️ **ВАЖНО:**
- Получите UUID разделов:
```
UUID_LUKS=$(blkid -s UUID -o значение "${DEV_RAIZ}")
UUID_ROOT=$(blkid -s UUID -o значение "${DISK}")
UUID_EFI=$(blkid -s UUID -o значение "${DEV_EFI}")
```
---

# ▶️ 14. Создать файл подкачки с поддержкой гибернации (необязательно)

### Важные примечания
```
- Файл подкачки в Btrfs всегда отображается как **prealloc**, это нормально.
- Это не обязательно должен быть полный размер оперативной памяти.
- 60% достаточно для гибернации в большинстве случаев.
- Для тяжелых грузов → используйте 70% или 80%.
```

1. Автоматический расчет оптимального размера файла подкачки.
```
# Современная рекомендация по спящему режиму: 60% от общего объема оперативной памяти.
SWAP_GB=$(LC_ALL=C awk '/MemTotal/ {print int($2 * 0,60/1024/1024)}' /proc/meminfo)
echo "Рекомендуемый файл подкачки: ${SWAP_GB}G"
```
- или вручную задайте нужный размер:
```
SWAP_GB=4
echo "Пользовательский файл подкачки: ${SWAP_GB}G"
```
2. Создайте каталог для файла подкачки.
```
mkdir -p /своп
swapoff -a 2>/dev/null
rm -f /своп/файл подкачки
```
3. Отключить COW (требуется в Btrfs)
```
чаттр +C /своп
```
4. Создайте файл подкачки заранее определенного размера.
```
Fallocate -l ${SWAP_GB}G /swap/файл подкачки
chmod 600 /своп/файл подкачки
```
5. Отформатируйте файл подкачки и активируйте подкачку.
```
mkswap /своп/файл подкачки
swapon /своп/файл подкачки
```
6. Получите компенсацию:
```
# Устанавливаем пакет для filefrag
xbps-install -Sy e2fsprogs

# Получить смещение
offset=$(filefrag -v /swap/swapfile | awk '/^ *0:/{print $4}')
```
---

# ▶️ 15. Настройте GRUB
⚠️ **ВАЖНО:**
> Этот блок умный:
- Автоматически определяет, используете ли вы LUKS
- Определяет, создали ли вы файл подкачки в спящем режиме.
- Настраивает /etc/default/grub, ничего не дублируя.
- Создает необходимые строки только в том случае, если они отсутствуют
- Ничего не меняйте, если в этом нет необходимости.

Используйте именно блок ниже:
```
HAS_RESUME=ложь
HAS_LUKS=ложь

[[ -n "${offset}" ]] && HAS_RESUME=true
[[ "${DISK}" = "${DEV_LUKS}" ]] && HAS_LUKS=true

# Удалить старую строку в целях безопасности
sed -i '/^[[:space:]]*GRUB_CMDLINE_LINUX_DEFAULT=/d' /etc/default/grub

# GRUB_CMDLINE_LINUX

# Базовое значение
BASE="уровень журнала=4"

# Добавить резюме
если $HAS_RESUME; затем
BASE="$BASE резюме=UUID=${UUID_ROOT} summary_offset=${offset}"
быть

# Добавить ЛУКИ
если $HAS_LUKS; затем
grep -q '^GRUB_ENABLE_CRYPTODISK=y' /etc/default/grub || echo 'GRUB_ENABLE_CRYPTODISK=y' >> /etc/default/grub
grep -q '^GRUB_PRELOAD_MODULES=' /etc/default/grub || echo 'GRUB_PRELOAD_MODULES="luks cryptodisk gcry_rijndael"' >> /etc/default/grub
BASE="$BASE rd.luks.uuid=${UUID_LUKS} rd.luks.name=${UUID_LUKS}=cryptroot root=/dev/mapper/cryptroot"
быть

# Правильно воссоздайте последнюю строку
echo "GRUB_CMDLINE_LINUX_DEFAULT=\"${BASE}\"" >> /etc/default/grub
```
---

# ▶️ 16. Воссоздать initrd
⚠️ **ВАЖНО:**
```
моды=(/usr/lib/modules/*)
KVER=$(базовое имя "${mods[0]}")
эхо ${KVER}
dracut --force --kver ${KVER}
```
---

# ▶️ 17. Создайте файл ключей, чтобы не запрашивать пароль дважды при загрузке (только LUKS)
> Если система НЕ ИСПОЛЬЗУЕТ LUKS, пропустите этот шаг.
```
если [ "${DISK}" = "${DEV_LUKS}"]; затем
echo "Обнаружен LUKS: создание ключевого файла для автоматической разблокировки..."

# Создать безопасный ключевой файл
dd if=/dev/urandom of=/boot/volume.key bs=64 count=1
chmod 000 /boot/volume.key

# Добавьте ключевой файл в LUKS (попросит ваш текущий пароль)
cryptsetup luksAddKey "${DEV_RAIZ}" /boot/volume.key

# Настраиваем /etc/crypttab
кот << EOF >> /etc/crypttab
cryptroot ${DEV_RAIZ} /boot/volume.key luks
ЭОФ

# Включаем ключевой файл и crypttab в initramfs
mkdir -p /etc/dracut.conf.d
кот << EOF >> /etc/dracut.conf.d/10-crypt.conf
install_items+=" /boot/volume.key /etc/crypttab "
ЭОФ

# Восстановить initramfs с поддержкой ключевых файлов
xbps-переконфигурировать -fa
еще
echo «Система без LUKS: пропуск создания ключевого файла».
быть
```

# ▶️    18. Instalar GRUB em **BIOS** e **UEFI** (híbrido real)
1. Установите GRUB для BIOS (устаревшая версия)
```
grub-install --target=i386-pc ${DEVICE}
```
2. Установите GRUB для UEFI.
```
grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=void
```
3. Создайте резервную версию UEFI (универсальную загрузку). Этот файл гарантирует загрузку даже при стирании NVRAM.
```
mkdir -p /boot/efi/EFI/BOOT
cp -f /boot/efi/EFI/void/grubx64.efi /boot/efi/EFI/BOOT/BOOTX64.EFI
```
4. Создайте окончательный файл GRUB.
```
grub-mkconfig -o /boot/grub/grub.cfg
```

---

# ▶️ 19. Индивидуальные настройки пользователя:

1. Настройки среды:

```
# Настраиваем /etc/xbps.d/00-repository-main.conf
mkdir -pv /etc/xbps.d
cat << 'EOF' >> /etc/xbps.d/00-repository-main.conf
репозиторий = https://repo-fastly.voidlinux.org/current
#repository=https://repo-fastly.voidlinux.org/current/nonfree
#repository=https://repo-fastly.voidlinux.org/current/multilib
#repository=https://repo-fastly.voidlinux.org/current/multilib/nonfree

репозиторий = https://void.chililinux.com/voidlinux/current
#repository=https://void.chililinux.com/voidlinux/current/extras
#repository=https://void.chililinux.com/voidlinux/current/nonfree
#repository=https://void.chililinux.com/voidlinux/current/multilib
#repository=https://void.chililinux.com/voidlinux/current/multilib/nonfree
ЭОФ

# Настройте /etc/rc.conf. Устанавливает часовой пояс консоли по умолчанию, раскладку клавиатуры и шрифт. Меняйте по мере необходимости.
кот << EOF >> /etc/rc.conf
TIMEZONE="${TIMEZONE}"
KEYMAP="${KEYMAP}"
FONT=Lat2-Terminus16
ЭОФ

# Настраиваем корневой .bashrc
wget --quiet --no-check-certificate \
-O /etc//skel/.bashrc \
"https://raw.githubusercontent.com/voidlinux-br/void-install/refs/heads/main/.bashrc"
корень chown: root /etc/skel/.bashrc
chmod 644 /etc/skel/.bashrc

cat << 'EOF' > /etc/skel/.bash_profile
# ~/.bash_profile — загружает .bashrc в Void

# Если .bashrc существует, загрузите
если [ -f ~/.bashrc ]; затем
источник ~/.bashrc
быть
ЭОФ

# копируем под root и пользователя
для d в /root "/home/${NEWUSER}"; делать
cp -f /etc/skel/.bash_profile "$d/"
cp -f /etc/skel/.bashrc "$d/"
сделанный

chown "${NEWUSER}:${NEWUSER}" "/home/${NEWUSER}/.bash_profile" "/home/${NEWUSER}/.bashrc"
chmod 644 "/home/${NEWUSER}/.bash_profile" "/home/${NEWUSER}/.bashrc"

# скачать пользовательский svlogtail
wget --quiet --no-check-certificate \
-O /usr/bin/svlogtail\
"https://raw.githubusercontent.com/voidlinux-br/void-install/refs/heads/main/svlogtail"
chmod +x /usr/bin/svlogtail
```

2. Настройте ssh (необязательно, но рекомендуется):
```
mkdir -pv /etc/ssh/sshd_config.d/
cat << 'EOF' > /etc/ssh/sshd_config.d/10-custom.conf
РазрешениеTTY да
ПринтМотд да
PrintLastLog да
Баннер /etc/issue.net

РазрешитьRootLogin да
KbdInteractiveAuthentication да
X11Переадресация да
PubkeyAuthentication да
PubkeyAcceptedKeyTypes=+ssh-rsa
AuthorizedKeysFile .ssh/authorized_keys
Аутентификация по паролю да
ВызовОтветАутентификация да
ИспользоватьPAM да

Подсистема sftp внутренний-sftp
ЭОФ
```
---

# ▶️ 20. Включить ZRAM (необязательно)
Void Linux использует службу zramen для включения ZRAM, создавая блок сжатой памяти, который уменьшает использование подкачки SSD и повышает производительность под нагрузкой.
1. Установить зрамен
```
xbps-install -Sy zramen
```
2. Настройте ZRAM (рекомендуемая конфигурация):
```
кот << 'EOF' > /etc/zramen.conf
zram_fraction=0,5
zram_devices=1
zram_algorithm=zstd
ЭОФ
```
3. Активируйте службу в runit.
```
пер -с
```
> ZRAM будет автоматически активироваться при каждой загрузке

---

# ▶️ 21. Завершить установку
1. Выполните chroot:
```
Выход
```
2. Отключите все разделы, смонтированные в /mnt (подтома и /boot/efi):
```
размонтировать -R /mnt
```
3. Отключите любой файл подкачки или раздел подкачки, активированный в chroot:
```
замена -а
```
4. Перезагрузите физическую машину или виртуальную машину, чтобы проверить фактическую загрузку:
```
перезагрузить
```
> Не забудьте извлечь установочный носитель и загрузиться с только что установленного диска.
Наслаждаться!

---

# 🎉 ПОЛНАЯ, ГИБРИДНАЯ, ПЕРЕГОРОДНАЯ СИСТЕМА
- Загрузочный биос + UEFI
- Резервный UEFI
- Btrfs со снимками (готовы для Snapper/Timeshift)
- Реальная гибернация с файлом подкачки
- Зрам за производительность

Этот SSD загружает **любую машину на планете**.

# ОТКАЗ ОТ ОТВЕТСТВЕННОСТИ

```
Это руководство бесплатное: вы можете использовать, копировать, изменять и распространять по своему усмотрению.
Содержимое предоставляется на условиях **Лицензии MIT** и может включать фрагменты или команды, полученные из программного обеспечения с открытым исходным кодом, на которое распространяются его собственные лицензии.

Никаких гарантий не предоставляется — здесь все поставляется «как есть».
Используйте на свой страх и риск. Ни автор, ни участники, ни Void Linux не несут ответственности за утрату, повреждение, системные сбои или любые последствия использования этого материала.

При желании вы можете получить исходный код, просмотреть, адаптировать и создать собственную версию этого руководства.
```

