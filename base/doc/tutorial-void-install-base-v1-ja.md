# 🔥 Base Void Linux インストールチュートリアル

# 始める前に

このチュートリアルでは、直接ディスク パーティショニング、「chroot」、および明示的なシステム構成を使用した、**Void Linux** の手動インストールについて説明します。
**自動インストーラー**ではありません。

## ⚠️よく読んでください

- このガイドは、**Linux**、ターミナル、および基本的なシステム概念 (ディスク、パーティション、ブート、サービス) に精通していることを前提としています。
- いくつかのコマンド **データを永久に削除** (`parted`、`mkfs`、`umount -R`)。
- ディスク (`/dev/sdX`、`/dev/nvmeX`) を定義するときにエラーが発生すると、**完全なデータ損失**が発生する可能性があります。
- **コマンドを実行する前に**チュートリアル全体をお読みください。

## 🖥️推奨環境

- **VM (VirtualBox、QEMU、KVM など)** テストと学習用。
- 専用ハードウェア **重要なデータはありません**。
- 実験室環境または意識の高い施設。

❌ **調整せずに運用環境で直接使用することは推奨されません**。

## 🔐 セキュリティについて

インストール プロセス中に、一部の設定ではセキュリティではなく **実用性が優先されます**。
- SSH 経由の「root」ユーザーのログインを一時的に有効にすることができます。
- パスワード認証が有効になっている可能性があります。
- レガシー互換性 (例: `ssh-rsa`) が許可される場合があります。

👉 **これらの設定は、特にネットワークに公開されているシステムでは、インストール後に確認する必要があります**。

## 🧠 知っておくべき重要なこと

- コマンドを **1 つずつ**実行し、出力を確認します。
- システムに応じて、ディスク名、ネットワーク インターフェイス、ユーザーを調整します。
- **むやみにコピーして貼り付けないでください**。
- 疑問がある場合は、**停止**して現在の手順を確認してください。

## 🛠️ エラーの場合

何か問題が発生した場合:
- むやみに再起動しないでください。
- パーティションを再組み立てします。
- `chroot` を使用してシステムに再度ログインします。
- GRUB、EFI、および「initramfs」をチェックします。

間違いを犯すこともその一部です。エラーを理解することがユーザーとオペレーターを分けるものです。

---

> このガイドは、古典的な Unix アプローチに従い、インストールよりも**フル コントロール**を好むユーザーを対象としています。
> **理解→設定→検証→続行**。

## インストールの開始
Void Linux ISO (x86_64 glibc または musl) から始めます。

1. root としてサインインします
```bash
ログイン: root
パスワード: voidlinux
```
2. シェルを *sh* から *bash* に切り替えます。
*dash/sh* **多くのスクリプトで使用されるいくつかの機能は実装されていません**。
```bash
バッシュ
```
3. キーボード レイアウトを **ABNT2** に変更し、アクセントと記号が正しくマッピングされるようにします。
```bash
ロードキー br-abnt2
```

4. インターネットに接続する
- **Wi-Fi** の場合 *(ケーブル接続の場合は、この手順をスキップしてください)*:
```bash
wpa_passphrase "WIFI_NETWORK_NAME" "NETWORK_PASSWORD" > wifi.conf
wpa_supplicant -B -i wlan0 -c wifi.conf
dhcpcd wlan0
```
> 📌 **注:** `wlan0` は異なる場合があります (`wlp2s0`、`wlp0s3` など)。
> 以下のコマンドを使用して、正しいインターフェイスを特定します。
>
>「バッシュ」
> ip -br a
>「」

5. 接続をテストします。
```bash
ping -c3 8.8.8.8
ping -c3 リポデフォルト.voidlinux.org
```

## SSH 経由で **root** ユーザーのログインを有効にします (オプション)。
この手順は、システムが VM で実行されている場合にのみ適用されます。ローカル ブート (VM なし) の場合、インストールはローカル ターミナル経由で通常どおり続行できます。
- これは、**ホスト**から VM にアクセスし、リモートでインストールを続行するために必要です。その後、SSH 経由でターミナルにコマンドを直接貼り付け/実行できます。

1. SSHの設定
```bash
echo 'PermitRootLogin はい' >> /etc/ssh/sshd_config
```
2. SSHサービスを再起動します。
```bash
SV 再起動 sshd
```

3. ネットワークインターフェースIPの表示
```bash
ip -4 ルート 1.1.1.1 を取得 | awk '{print $7}'
```
>ネットワーク インターフェイスの IP をメモし、それを使用して SSH 経由で VM に接続します。

4. ホストから SSH 経由で VM にアクセスします。
```bash
sudo ssh <ip-da-vm>
```
> デフォルトのパスワード: `voidlinux`

## ターミナルで色付きのプロンプトを構成します (オプション)
ユーザー、ホスト、現在のパス、および最後のコマンドのステータスが表示されます。
```bash
エクスポート PROMPT_COMMAND='RET=$?'
import PS1='\[\e[1;33m\]\u\[\e[0m\]@\[\e[1;35m\]\h\[\e[0m\]:\[\e[0;37m\]\w\[\e[0m\] \[\e[1;32m\]$( [ $RET -eq 0 ] && printf ✔ || printf "\e[1;31m✘$RET" )\[\e[0m\] \$ '
```
> 📌 このプロンプトは現在のセッションでのみ有効です。永続的にするには、それを `.bashrc` に追加します。

## 必要なパッケージをインストールする
⚠️ **重要:**
```bash
xbps-install -Sy xbps parted nano vim zstd xz bash-completion
```

## ディスクのパーティションを作成する
1. ディスクを識別する
```bash
fdisk -l | grep -E '^(ディスク|ディスク) '
```
> チュートリアルでは `/dev/sda` を想定します

2. 使用するディスクに応じて、以下の変数を調整します (**重要**)。
```bash
# SATA/SCSI ディスク (sdX)
エクスポート DEVICE=/dev/sda
エクスポート DEV_EFI=${DEVICE}2
エクスポート DEV_ROOT=${DEVICE}3
```

> 📌 **注:**
> **NVMe** ディスクの場合、パーティションのサフィックスが変更されます (`p`):
>「バッシュ」
> エクスポート DEVICE=/dev/nvme0n1
> エクスポート DEV_EFI=${DEVICE}p2
> エクスポート DEV_RAIZ=${DEVICE}p3
>「」

3. **parted** (自動モード) を使用してディスクをパーティション分割します。
このスキームにより以下が作成されます。
- BIOS パーティション (bios_grub)
- EFI パーティション (ESP)
- ルートパーティション(ROOT)
```bash
winefs -a "${DEVICE}"
分割 --script "${DEVICE}" -- \
mklabel gpt \
mkpart プライマリ 1MiB 2MiB 名 1 BIOS セット \ 上の 1 bios_grub
mkpart プライマリ fat32 2MiB 514MiB 名前 2 EFI セット 2 esp on \
mkpart プライマリ 514MiB 100% 名前 3 ROOT \
最適な整列チェック 1 \
最適な整列チェック 2 \
最適な整列チェック 3
parted --script "${DEVICE}" -- print
```

## パーティションをフォーマットする
```bash
# ルートパーティション（ext4）をフォーマットします
mkfs.ext4 -F ${DEV_RAIZ}

# EFI パーティションをフォーマット (FAT32)
mkfs.fat -F32 -I ${DEV_EFI}
```

## ボリュームを `/mnt` にマウントします
```bash
# ルートパーティションをマウントする
マウント ${DEV_RAIZ} /mnt

# 必要なマウントポイントを作成する
/t /mnt/{hame,boot/efi,var/log,var/cache, proced, proc, Proc,)

# EFI パーティションをマウントする
マウント ${DEV_EFI} /mnt/boot/efi
```

## 基本システムをインストールする
カーネル、ファームウェア、ブートローダー、ネットワーキング、および必須ツールを含む、Void Linux ベース システムを「/mnt」マウント環境にインストールします。
```bash
xbps-install -Sy -R https://repo-default.voidlinux.org/current \
-r /mnt \
ベースシステム e2fsprogs grub-x86_64-efi dracut linux \
linux-headers linux-firmware linux-firmware-network glibc-locales \
xtools dhcpcd openssh vim nano grc zstd xz bash-completion vpm vsv \
socklog-void wget net-tools tmate ncurses chrony
```

> 📌 **注:**
> - `grub-x86_64-efi` → ブートローダー UEFI
> - `linux` → カーネル
> - `linux-firmware-network` → ネットワークドライバー
> - `xtools` → `xgenfstab`を必ず使用する必要がある

## `fstab` を作成します
システムの永続マウント ファイルを自動的に生成します。
```bash
xgenfstab -U /mnt > /mnt/etc/fstab
```

## エントリとシステム (chroot)
`/mnt` にインストールされているシステムにアクセスして構成を続行します。
```bash
xchroot /mnt /bin/bash
```

## INITRAMFS を生成する
仮想化環境用の dracut 構成 (VM セーフ)
```bash
cat > /etc/dracut.conf.d/99-vm-safe.conf << 'EOF'
# /etc/dracut.conf.d/99-vm-safe.conf
ホストのみ=いいえ
圧縮 = "gzip"
add_drivers+=" virtio virtio_pci virtio_blk virtio_net virtio_scsi "
終了後
```

インストールされているカーネルのバージョンを自動的に検出し、**dracut** を使用して対応する `initramfs` を生成します。
```bash
mods=(/usr/lib/modules/*)
KVER=$(ベース名 "${mods[0]}")
エコー ${KVER}
dracut --force --kver ${KVER}
```

## GRUB を設定する

> 📌 両方の方法 (BIOS と UEFI) は意図的にインストールされます。
> これにより、**レガシー BIOS** と **UEFI** システムの両方で同じディスクを起動できるようになり、マシン間の移植性が向上します。

1. GRUB サポート ディレクトリを作成します。
```bash
mkdir -p /ブート/grub
```

2. **BIOS (レガシー)** 用の GRUB をインストールします。
```bash
grub-install --target=i386-pc ${DEVICE}
```

3. **UEFI** 用の GRUB をインストールします。
```bash
grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=void
```

4. UEFI フォールバック (ユニバーサル ブート) を作成します。
このファイルは、NVRAM が消去された場合でもブートを保証します。
```bash
mkdir -p /boot/efi/EFI/BOOT
cp -f /boot/efi/EFI/void/grubx64.efi /boot/efi/EFI/BOOT/BOOTX64.EFI
```

5. 最終的な GRUB 構成ファイルを生成します。
```bash
grub-mkconfig -o /boot/grub/grub.cfg
```

## ユーザーの作成と構成

⚠️ **重要:** 以下に実際のユーザー名を定義します。
```bash
NEWUSER=your_user_here をエクスポート
```

ホーム ディレクトリ、基本グループ、および Bash シェルを使用してユーザーを作成します。
```bash
useradd -m -G オーディオ、ビデオ、ホイール、tty -s /bin/bash ${NEWUSER}
```

ユーザーパスワードを設定します (***重要***)
```bash
パスワード ${NEWUSER}
```

root ユーザーのパスワードを設定します (***重要***)
```bash
パスワードルート
```

root ユーザーのデフォルトのシェルを Bash に変更する
```bash
chsh -s /bin/bash root
```

## 基本設定
```bash
# ホスト名を設定する
echo void > /etc/ホスト名

# 現地時間を設定する
ln -sf /usr/share/zoneinfo/America/Sao_Paulo /etc/localtime

# セタールローカル
sed -i 's/#en_US.UTF-8/en_US.UTF-8/' /etc/default/libc-locales
sed -i 's/#pt_BR.UTF-8/pt_BR.UTF-8/' /etc/default/libc-locales

# ロケールを生成します:
xbps-reconfigure -f glibc-locales

# /var/service シンボリックリンクで考えられるエラーを修正します (重要):
rm -f /var/サービス
ln -sf /etc/runit/runsvdir/default /var/service

# いくつかのサービスをアクティブ化します。
ln -sf /etc/sv/dbus /var/service/
ln -sf /etc/sv/dhcpcd /var/service/
ln -sf /etc/sv/sshd /var/service/
ln -sf /etc/sv/nanoklogd /var/service/
ln -sf /etc/sv/socklog-unix /var/service/
ln -sf /etc/sv/chronyd /var/service/

# カスタム svlogtail をダウンロードします (オプションですが推奨):
wget --quiet --no-check-certificate -O /usr/bin/svlogtail \
「https://raw.githubusercontent.com/voidlinux-br/void-install/refs/heads/main/svlogtail" && \
chmod +x /usr/bin/svlogtail

# resolv.confを作成する
printf 'ネームサーバー 1.1.1.1\nネームサーバー 8.8.8.8\n' > /etc/resolv.conf

#sudo の構成 - Wheel グループ (オプションですが推奨)
cat << 'EOF' > /etc/sudoers.d/g_wheel
%wヒール ALL=(ALL:ALL) NOPASSWD: ALL
終了後

#必要な権限
chmod 440 /etc/sudoers.d/g_wheel
```

## `/etc/xbps.d/00-repository-main.conf` をカスタマイズします
*(オプションですが推奨)*

**XBPS** 構成ディレクトリを作成し (まだ存在しない場合)、公式リポジトリと代替リポジトリのリストを定義します。
**repo-fastly** リポジトリのレイテンシは長くなる傾向があります。

```bash
mkdir -pv /etc/xbps.d

cat << 'EOF' > /etc/xbps.d/00-repository-main.conf
# 公式リポジトリ (高速 – 最高のレイテンシ)
リポジトリ=https://repo-fastly.voidlinux.org/current
#repository=https://repo-fastly.voidlinux.org/current/nonfree
#repository=https://repo-fastly.voidlinux.org/current/multilib
#repository=https://repo-fastly.voidlinux.org/current/multilib/nonfree

# 代替リポジトリ (Chili Linux)
リポジトリ=https://void.chililinux.com/voidlinux/current
#repository=https://void.chililinux.com/voidlinux/current/extras
#repository=https://void.chililinux.com/voidlinux/current/nonfree
#repository=https://void.chililinux.com/voidlinux/current/multilib
#repository=https://void.chililinux.com/voidlinux/current/multilib/nonfree
終了後
```

## `/etc/rc.conf` をカスタマイズする
本体のデフォルトのタイムゾーン、キーボードレイアウト、フォントを設定します。
必要に応じて変更します。
```bash
cat << 'EOF' > /etc/rc.conf
TIMEZONE=アメリカ/サンパウロ
KEYMAP=br-abnt2
FONT=Lat2-Terminus16
終了後
```

Virtio モジュール (仮想マシン)。
```bash
cat > /etc/modules-load.d/virtio.conf << 'EOF'
ヴィルティオ
virtio_pci
virtio_net
virtio_blk
virtio_scsi
終了後
```

## ユーザー `.bashrc` をカスタマイズする
デフォルトの `.bash_profile` を作成し、ログイン時に `.bashrc` が自動的にロードされるようにします。
> ⚠️ ユーザーが前のステップで作成されたことを確認してください。

```bash
# デフォルトの .bashrc を /etc/skel にダウンロードします
wget --quiet --no-check-certificate \
-O /etc/skel/.bashrc \
「https://raw.githubusercontent.com/voidlinux-br/void-install/refs/heads/main/.bashrc"」

chown root:root /etc/skel/.bashrc
chmod 644 /etc/skel/.bashrc

# デフォルトの .bash_profile を作成する
cat << 'EOF' > /etc/skel/.bash_profile
# ~/.bash_profile — .bashrc を Void にロードします

# .bashrcが存在する場合はロード
if [ -f ~/.bashrc ];それから
ソース ~/.bashrc
なれ
終了後

# root とユーザーにコピーします
/root "/home/${NEWUSER}" の d の場合;する
cp -f /etc/skel/.bash_profile "$d/"
cp -f /etc/skel/.bashrc "$d/"
終わり

# ユーザー権限を調整する
chown "${NEWUSER}:${NEWUSER}" \
"/home/${NEWUSER}/.bash_profile" \
「/home/${NEWUSER}/.bashrc」

chmod 644 \
"/home/${NEWUSER}/.bash_profile" \
「/home/${NEWUSER}/.bashrc」
```

## SSH を構成する
*(オプションですが推奨)*

メインファイルはそのままにして、**sshd** の補足構成ファイルを作成します。
```bash
mkdir -pv /etc/ssh/sshd_config.d

cat << 'EOF' > /etc/ssh/sshd_config.d/10-custom.conf
# 一般設定
TTY を許可します はい
PrintMotd はい
最終ログの印刷はい
バナー /etc/issue.net

# 認証
PermitRootLogin はい
パスワード認証はい
KbdInteractiveAuthentication はい
チャレンジレスポンス認証はい
公開鍵認証はい
PubkeyAcceptedKeyTypes=+ssh-rsa
AuthorizedKeysファイル .ssh/authorized_keys
PAM を使用する はい

＃ 特徴
X11転送はい
サブシステム sftp 内部 sftp
終了後
```
> ⚠️ 特にインターネットに公開されているシステムでは、最初の起動後にこれらの SSH 設定を確認して強化することをお勧めします。

## インストールを完了する
サイア・ド・クルート
```bash
出口
```
`/mnt` にマウントされているすべてのパーティション (サブボリュームと `/boot/efi` を含む) をアンマウントします。
```bash
umount -R /mnt
```
物理マシンまたは VM を再起動して、実際のブートをテストします。
```bash
リブート
```
> 📌 **注: インストール メディアを削除することを忘れないでください。

＃🎉楽しんでください！
**Void Linux** がインストールされ、使用できるようになりました。

# 免責事項

> このチュートリアルは無料です。自由に使用、コピー、変更、再配布できます。
> コンテンツは **MIT ライセンス** に基づいて利用可能であり、独自のライセンスの対象となる、オープン ソース ソフトウェアから派生した抜粋やコマンドが含まれる場合があります。
>
> 保証は提供されません。ここにあるものはすべて **「現状のまま」** で提供されます。
> ご自身の責任でご使用ください。著者も寄稿者も、Void Linux も、このマテリアルの使用による損失、損傷、システム障害、またはいかなる結果に対しても責任を負いません。
>
> このチュートリアルの独自のバージョンを自由に確認、調整、生成できます。

