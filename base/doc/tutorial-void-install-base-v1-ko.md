# 🔥 Base Void Linux 설치 튜토리얼

# 시작하기 전에

이 튜토리얼에서는 직접 디스크 파티셔닝, `chroot` 및 명시적인 시스템 구성을 사용하여 **Void Linux의 수동 설치**에 대해 설명합니다.
**자동 설치 프로그램이 아닙니다**.

## ⚠️ 잘 읽어보세요

- 이 가이드는 **Linux**, 터미널 및 기본 시스템 개념(디스크, 파티션, 부팅, 서비스)에 익숙하다고 가정합니다.
- 여러 명령 **영구적으로 데이터를 삭제**합니다(`parted`, `mkfs`, `umount -R`).
- 디스크(`/dev/sdX`, `/dev/nvmeX`)를 정의할 때 오류가 발생하면 **전체 데이터 손실**이 발생할 수 있습니다.
- **명령을 실행하기 전에 전체 튜토리얼**을 읽어보세요.

## 🖥️ 권장 환경

- **VM(VirtualBox, QEMU, KVM 등)** 테스트 및 학습용.
- 전용 하드웨어 **중요한 데이터 없음**.
- 실험실 환경 또는 의식이 있는 시설.

❌ 개조 없이 프로덕션에 직접 사용하는 것은 **권장하지 않습니다**.

## 🔐 보안 정보

설치 프로세스 중 일부 설정은 보안이 아닌 **실용성을 우선시**합니다.
- SSH를 통한 `root` 사용자 로그인이 일시적으로 활성화될 수 있습니다.
- 비밀번호 인증이 활성화되어 있을 수 있습니다.
- 레거시 호환성(예: `ssh-rsa`)이 허용될 수 있습니다.

👉 **이러한 설정은 설치 후 검토해야 합니다**, 특히 네트워크에 노출된 시스템에서는 더욱 그렇습니다.

## 🧠 알아두어야 할 중요 사항

- 명령을 **하나씩** 실행하여 출력을 확인합니다.
- 시스템에 따라 디스크 이름, 네트워크 인터페이스 및 사용자를 조정합니다.
- **무작정 복사해서 붙여넣지 마세요**.
- 의심스러운 경우 **중지**하고 현재 단계를 검토하세요.

## 🛠️ 오류가 발생한 경우

문제가 발생하는 경우:
- 맹목적으로 다시 시작하지 마십시오.
- 파티션을 다시 조립하세요.
- `chroot`를 사용하여 시스템에 다시 로그인합니다.
- GRUB, EFI 및 `initramfs`를 확인하세요.

실수하는 것도 그것의 일부입니다. 오류를 이해하는 것이 사용자와 운영자를 구분하는 요소입니다.

---

> 이 가이드는 고전적인 Unix 접근 방식에 따라 설치에 대한 **완전한 제어**를 선호하는 사용자를 대상으로 합니다.
> **이해 → 구성 → 검증 → 계속**.

## 설치 시작
Void Linux ISO(x86_64 glibc 또는 musl)로 시작하세요.

1. 루트로 로그인
```bash
로그인 : 루트
비밀번호 : voidlinux
```
2. 쉘을 *sh*에서 *bash*로 전환합니다.
*대시/sh* 많은 스크립트에서 사용하는 여러 기능을 **구현하지 않습니다**.
```bash
세게 때리다
```
3. 키보드 레이아웃을 **ABNT2**로 변경하여 악센트와 기호가 올바르게 매핑되도록 합니다.
```bash
로드키 br-abnt2
```

4. 인터넷에 연결
- **Wi-Fi** *(케이블을 사용하는 경우 이 단계 건너뛰기)*:
```bash
wpa_passphrase "WIFI_NETWORK_NAME" "NETWORK_PASSWORD" > wifi.conf
wpa_supplicant -B -i wlan0 -c wifi.conf
dhcpcd wlan0
```
> 😀 **참고:** `wlan0`은 다를 수 있습니다(`wlp2s0`, `wlp0s3` 등).
> 올바른 인터페이스를 식별하려면 아래 명령을 사용하십시오.
>
>``배쉬
> ip -br a
>````

5. 연결을 테스트합니다.
```bash
핑 -c3 8.8.8.8
ping -c3 repo-default.voidlinux.org
```

## SSH를 통해 **root** 사용자 로그인을 활성화합니다(선택 사항).
이 단계는 시스템이 VM에서 실행 중인 경우에만 적용됩니다. 로컬 부팅(VM 없이)의 경우 로컬 터미널을 통해 정상적으로 설치가 진행될 수 있습니다.
- **호스트에서 VM**에 액세스하고 원격으로 설치를 계속하는 데 필요합니다. 그 후 SSH를 통해 터미널에 직접 명령을 붙여넣거나 실행할 수 있습니다.

1. SSH 구성
```bash
echo 'PermitRootLogin 예' >> /etc/ssh/sshd_config
```
2. SSH 서비스를 다시 시작하세요
```bash
sv sshd 다시 시작
```

3. 네트워크 인터페이스 IP 보기
```bash
ip -4 경로는 1.1.1.1을 얻습니다 | awk '{인쇄 $7}'
```
> 네트워크 인터페이스의 IP를 기록하고 이를 사용하여 SSH를 통해 VM에 연결합니다.

4. 호스트에서 SSH를 통해 VM에 액세스합니다.
```bash
sudo ssh <ip-da-vm>
```
> 기본 비밀번호: `voidlinux`

## 터미널에서 색상 프롬프트 구성(선택 사항)
사용자, 호스트, 현재 경로 및 마지막 명령의 상태가 표시됩니다.
```bash
PROMPT_COMMAND='RET=$?' 내보내기
내보내기 PS1='\[\e[1;33m\]\u\[\e[0m\]@\[\e[1;35m\]\h\[\e[0m\]:\[\e[0;37m\]\w\[\e[0m\] \[\e[1;32m\]$( [ $RET -eq 0 ] && printf ✔ || printf "\e[1;31m✘$RET" )\[\e[0m\] \$ '
```
> 😀 이 프롬프트는 현재 세션에만 유효합니다. 영구적으로 만들려면 `.bashrc`에 추가하세요.

## 필수 패키지 설치
⚠️ **중요:**
```bash
xbps-install -Sy xbps parted nano vim zstd xz bash-완성
```

## 디스크 파티션하기
1. 디스크 식별
```bash
fdisk -l | grep -E '^(디스크|디스크) '
```
> 튜토리얼 `/dev/sda`를 가정하겠습니다.

2. 사용할 디스크에 따라 아래 변수를 조정합니다(**중요**):
```bash
# SATA/SCSI 디스크(sdX)
장치 내보내기=/dev/sda
DEV_EFI=${DEVICE}2 내보내기
DEV_ROOT=${DEVICE}3 내보내기
```

> 😀 **참고:**
> **NVMe** 디스크의 경우 파티션 접미사가 변경됩니다(`p`).
>``배쉬
> 장치 내보내기=/dev/nvme0n1
> DEV_EFI=${DEVICE}p2 내보내기
> DEV_RAIZ=${DEVICE}p3 내보내기
>````

3. **parted**(자동 모드)를 사용하여 디스크를 분할합니다.
이 구성표는 다음을 생성합니다.
- BIOS 파티션(bios_grub)
- EFI 파티션(ESP)
- 루트 파티션(ROOT)
```bash
와이프 -a "${DEVICE}"
parted --script "${DEVICE}" -- \
mklabel gpt \
mkpart 기본 1MiB 2MiB 이름 1 BIOS 세트 1 bios_grub on \
mkpart 기본 fat32 2MiB 514MiB 이름 2 EFI 세트 2 esp on \
mkpart 기본 514MiB 100% 이름 3 ROOT \
정렬-검사 최적 1 \
정렬-검사 최적 2 \
정렬 확인 최적 3
parted --script "${DEVICE}" -- 인쇄
```

## 파티션 포맷
```bash
# 루트 파티션을 포맷합니다(ext4)
mkfs.ext4 -F ${DEV_RAIZ}

# EFI 파티션(FAT32)을 포맷합니다.
mkfs.fat -F32 -I ${DEV_EFI}
```

## `/mnt`에 볼륨을 마운트합니다.
```bash
# 루트 파티션을 마운트합니다.
${DEV_RAIZ} /mnt 마운트

# 필요한 마운트 지점을 생성합니다
/t /mnt/{hame,boot/efi,var/log,var/cache, proced, proc, Proc,)

# EFI 파티션 마운트
마운트 ${DEV_EFI} /mnt/boot/efi
```

## 기본 시스템 설치
커널, 펌웨어, 부트로더, 네트워킹 및 필수 도구를 포함하여 Void Linux 기본 시스템을 '/mnt' 마운트 환경에 설치합니다.
```bash
xbps-설치 -Sy -R https://repo-default.voidlinux.org/current \
-r /mnt \
기본 시스템 e2fsprogs grub-x86_64-efi dracut linux \
리눅스 헤더 리눅스 펌웨어 리눅스 펌웨어 네트워크 glibc-로케일 \
xtools dhcpcd openssh vim nano grc zstd xz bash-completion vpm vsv \
socklog-void wget net-tools tmate ncurses 크로니
```

> 😀 **참고:**
> - `grub-x86_64-efi` → 부트로더 UEFI
> - `linux` → 커널
> - `linux-firmware-network` → 네트워크 드라이버
> - `xtools` → `xgenfstab`을 반드시 사용하기 위해 필요

## `fstab` 생성
시스템의 영구 마운트 파일을 자동으로 생성합니다.
```bash
xgenfstab -U /mnt > /mnt/etc/fstab
```

## 엔트라 및 시스템(chroot)
구성을 계속하려면 `/mnt`에 설치된 시스템에 액세스하세요.
```bash
xchroot /mnt /bin/bash
```

## INITRAMFS 생성
가상화 환경을 위한 Dracut 구성(VM 안전)
```bash
고양이 > /etc/dracut.conf.d/99-vm-safe.conf << 'EOF'
# /etc/dracut.conf.d/99-vm-safe.conf
호스트만=아니요
압축="gzip"
add_drivers+=" virtio virtio_pci virtio_blk virtio_net virtio_scsi "
EOF
```

설치된 커널 버전을 자동으로 감지하고 **dracut**을 사용하여 해당 `initramfs`를 생성합니다.
```bash
모드=(/usr/lib/모듈/*)
KVER=$(기본 이름 "${mods[0]}")
에코 ${KVER}
dracut --force --kver ${KVER}
```

## GRUB 구성

> 😀 두 가지 방법(BIOS, UEFI)은 일부러 설치한 것입니다.
> 이를 통해 **레거시 BIOS** 및 **UEFI** 시스템 모두에서 동일한 디스크를 부팅할 수 있어 시스템 간 이동성이 향상됩니다.

1. GRUB 지원 디렉터리를 생성합니다:
```bash
mkdir -p /boot/grub
```

2. **BIOS(레거시)**용 GRUB를 설치합니다.
```bash
grub-install --target=i386-pc ${DEVICE}
```

3. **UEFI**용 GRUB를 설치합니다.
```bash
grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=void
```

4. UEFI 폴백(범용 부팅)을 생성합니다.
이 파일은 NVRAM이 지워지더라도 부팅을 보장합니다.
```bash
mkdir -p /boot/efi/EFI/BOOT
cp -f /boot/efi/EFI/void/grubx64.efi /boot/efi/EFI/BOOT/BOOTX64.EFI
```

5. 최종 GRUB 구성 파일을 생성합니다.
```bash
grub-mkconfig -o /boot/grub/grub.cfg
```

## 사용자 생성 및 구성

⚠️ **중요:** 아래에서 실제 사용자 이름을 정의하세요.
```bash
NEWUSER=your_user_here로 내보내기
```

홈 디렉토리, 기본 그룹 및 Bash 쉘을 사용하여 사용자를 작성하십시오.
```bash
useradd -m -G 오디오, 비디오, 휠, tty -s /bin/bash ${NEWUSER}
```

사용자 비밀번호 설정(***중요***)
```bash
비밀번호 ${NEWUSER}
```

루트 사용자 비밀번호 설정(***중요***)
```bash
비밀번호 루트
```

루트 사용자의 기본 셸을 Bash로 변경
```bash
chsh -s /bin/bash 루트
```

## 기본 설정
```bash
# Setar 호스트 이름
echo void > /etc/호스트 이름

# 현지시간 설정
ln -sf /usr/share/zoneinfo/America/Sao_Paulo /etc/localtime

# 세타르 로컬
sed -i 's/#en_US.UTF-8/en_US.UTF-8/' /etc/default/libc-locales
sed -i 's/#pt_BR.UTF-8/pt_BR.UTF-8/' /etc/default/libc-locales

# 로케일을 생성합니다:
xbps-재구성 -f glibc-로케일

# /var/service 심볼릭 링크에서 발생할 수 있는 오류를 수정합니다(중요):
rm -f /var/서비스
ln -sf /etc/runit/runsvdir/default /var/service

# 일부 서비스를 활성화합니다:
ln -sf /etc/sv/dbus /var/service/
ln -sf /etc/sv/dhcpcd /var/service/
ln -sf /etc/sv/sshd /var/service/
ln -sf /etc/sv/nanoklogd /var/service/
ln -sf /etc/sv/socklog-unix /var/service/
ln -sf /etc/sv/chronyd /var/service/

# 사용자 정의 svlogtail을 다운로드합니다(선택 사항이지만 권장됨):
wget --quiet --no-check-certificate -O /usr/bin/svlogtail \
"https://raw.githubusercontent.com/voidlinux-br/void-install/refs/heads/main/svlogtail" && \
chmod +x /usr/bin/svlogtail

# resolv.conf 생성
printf '네임서버 1.1.1.1\n네임서버 8.8.8.8\n' > /etc/resolv.conf

#Sudo - 휠 그룹 구성(선택 사항이지만 권장됨)
고양이 << 'EOF' > /etc/sudoers.d/g_wheel
%wheel ALL=(ALL:ALL) NOPASSWD: ALL
EOF

#필수권한
chmod 440 /etc/sudoers.d/g_wheel
```

## `/etc/xbps.d/00-repository-main.conf` 사용자 정의
*(선택사항이지만 권장됨)*

**XBPS** 구성 디렉터리(아직 없는 경우)를 만들고 공식 및 대체 저장소 목록을 정의합니다.
**repo-fastly** 저장소는 대기 시간이 더 좋은 경향이 있습니다.

```bash
mkdir -pv /etc/xbps.d

고양이 << 'EOF' > /etc/xbps.d/00-repository-main.conf
# 공식 저장소(Fastly – 최고의 대기 시간)
저장소=https://repo-fastly.voidlinux.org/current
#repository=https://repo-fastly.voidlinux.org/current/nonfree
#repository=https://repo-fastly.voidlinux.org/current/multilib
#repository=https://repo-fastly.voidlinux.org/current/multilib/nonfree

# 대체 저장소(칠리 리눅스)
저장소=https://void.chililinux.com/voidlinux/current
#repository=https://void.chililinux.com/voidlinux/current/extras
#repository=https://void.chililinux.com/voidlinux/current/nonfree
#repository=https://void.chililinux.com/voidlinux/current/multilib
#repository=https://void.chililinux.com/voidlinux/current/multilib/nonfree
EOF
```

## `/etc/rc.conf` 사용자 정의
콘솔의 기본 시간대, 키보드 레이아웃 및 글꼴을 설정합니다.
필요에 따라 변경하세요.
```bash
고양이 << 'EOF' > /etc/rc.conf
TIMEZONE=미국/Sao_Paulo
키맵=br-abnt2
글꼴=Lat2-Terminus16
EOF
```

Virtio 모듈(가상 머신).
```bash
고양이 > /etc/modules-load.d/virtio.conf << 'EOF'
비르티오
virtio_pci
virtio_net
virtio_blk
virtio_scsi
EOF
```

## 사용자 `.bashrc` 사용자 정의
기본 `.bash_profile`을 생성하고 로그인 시 `.bashrc`가 자동으로 로드되도록 합니다.
> ⚠️ 이전 단계에서 사용자가 생성되었는지 확인하세요.

```bash
# 기본 .bashrc를 /etc/skel에 다운로드합니다.
wget --quiet --no-check-certificate \
-O /etc/skel/.bashrc \
"https://raw.githubusercontent.com/voidlinux-br/void-install/refs/heads/main/.bashrc"

chown 루트:루트 /etc/skel/.bashrc
chmod 644 /etc/skel/.bashrc

# 기본 .bash_profile 생성
고양이 << 'EOF' > /etc/skel/.bash_profile
# ~/.bash_profile — .bashrc를 Void에 로드합니다.

# .bashrc가 존재하면 로드합니다.
if [ -f ~/.bashrc ]; 그 다음에
소스 ~/.bashrc
BE
EOF

# 루트와 사용자에게 복사
/root "/home/${NEWUSER}"의 d에 대해; 하다
cp -f /etc/skel/.bash_profile "$d/"
cp -f /etc/skel/.bashrc "$d/"
완료

# 사용자 권한 조정
chown "${NEWUSER}:${NEWUSER}" \
"/home/${NEWUSER}/.bash_profile" \
"/홈/${NEWUSER}/.bashrc"

chmod 644 \
"/home/${NEWUSER}/.bash_profile" \
"/홈/${NEWUSER}/.bashrc"
```

## SSH 구성
*(선택사항이지만 권장됨)*

기본 파일은 그대로 두고 **sshd**에 대한 추가 구성 파일을 생성합니다.
```bash
mkdir -pv /etc/ssh/sshd_config.d

고양이 << 'EOF' > /etc/ssh/sshd_config.d/10-custom.conf
# 일반 설정
PermitTTY 예
PrintMotd 예
PrintLastLog 예
배너 /etc/issue.net

# 인증
PermitRoot로그인 예
비밀번호인증 예
KbdInteractiveAuthentication 예
ChallengeResponse인증 예
Pubkey인증 예
PubkeyAcceptedKeyTypes=+ssh-rsa
AuthorizedKeys파일 .ssh/authorized_keys
PAM을 사용하세요 예

# 특징
X11전달 예
하위 시스템 SFTP 내부 SFTP
EOF
```
> ⚠️ 특히 인터넷에 노출된 시스템에서는 처음 부팅한 후 이러한 SSH 설정을 검토하고 강화하는 것이 좋습니다.

## 설치 마무리
사이어 두 chroot
```bash
출구
```
`/mnt`(하위 볼륨 및 `/boot/efi` 포함)에 마운트된 모든 파티션을 마운트 해제합니다.
```bash
umount -R /mnt
```
실제 부팅을 테스트하려면 물리적 머신이나 VM을 다시 시작하세요.
```bash
재부팅
```
> 😀 **참고: 설치 미디어를 제거하는 것을 잊지 마세요.

# 🎉 즐겨보세요!
**Void Linux**가 이제 설치되었으며 사용할 준비가 되었습니다.

# 면책조항

> 이 튜토리얼은 무료입니다. 원하는 대로 사용, 복사, 수정 및 재배포할 수 있습니다.
> 콘텐츠는 **MIT 라이선스**에 따라 제공되며 자체 라이선스에 따라 오픈 소스 소프트웨어에서 파생된 발췌문이나 명령이 포함될 수 있습니다.
>
> 어떠한 보증도 제공되지 않습니다. 여기에 있는 모든 내용은 **“있는 그대로”** 제공됩니다.
> 사용에 따른 책임은 사용자 본인에게 있습니다. 저자, 기여자, Void Linux 모두 이 자료의 사용으로 인한 손실, 손상, 시스템 오류 또는 결과에 대해 책임을 지지 않습니다.
>
> 이 튜토리얼의 자신만의 버전을 자유롭게 검토하고, 조정하고, 생성할 수 있습니다.

