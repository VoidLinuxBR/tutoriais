# Учебное пособие Distrobox no Void Linux

Distrobox позволяет вам использовать другие дистрибутивы Linux в вашем Void Linux.
без ущерба для базовой системы (хоста).
Логика обычная: **чистая система, изолированные тесты, ноль обходных путей**.

---

## Какое здесь предложение

Установите дистрибутив на Void Linux и используйте контейнеры для безопасного запуска других дистрибутивов.

Это исключает риск:
- разорвать системные зависимости
- загрязнять хост пакетами из других дистрибутивов
- превратить Пустоту во Франкенштейна

Примечание. В этом руководстве предполагается, что Void Linux уже установлен.

---

## Прежде всего: репозиторий chililinux.

Пакет `distrobox` не существует в официальных репозиториях Void.
Но он был упакован сообществом VoidLinuxBR, поэтому необходимо добавить репозиторий chililinux (официальное зеркало Void в Бразилии — <https://xmirror.voidlinux.org/>).).

Выполните **точно** команды ниже:
```bash
sudo sh -c "{
  echo 'repository=https://repo-fastly.voidlinux.org/current'
  echo 'repository=https://void.chililinux.com/voidlinux/current'
} > /etc/xbps.d/00-repository-main.conf"
```

---

## Обновление базовой системы

Прежде чем устанавливать что-либо, убедитесь, что ваша система обновлена:

```bash
sudo xbps-install -Syu xbps
sudo xbps-install -Syu libssh2 xtools
sudo xbps-install -Suy
xcheckrestart
```
Если `xcheckrestart` указывает на перезагрузку, перезапустите.

---

## Установка Distrobox и зависимостей

Теперь установите необходимые пакеты:

```bash
sudo xbps-install -Syf voidbr-distrobox podman docker crun
```

Важный:
После установки crun обязательно перезагрузите систему:

```bash
sudo reboot
```

---

## О совместимости дистрибутива

Не каждый дистрибутив хорошо работает в контейнерах.
Прежде чем сделать выбор, ознакомьтесь с официальным списком:

https://distrobox.it/compatibility/#containers-distros

Это позволяет избежать напрасной траты времени и головной боли.

---

## Создание первого контейнера (Debian)

В качестве примера будет использоваться тестирование Debian.

```bash
distrobox create -Y --name debian --image docker.io/library/debian:testing
```

Что здесь происходит:
- `distrobox create` создает контейнер
- `-Y` позволяет избежать интерактивных вопросов
- `--name` определяет имя контейнера
- `--image` определяет базовое изображение

Чтобы просмотреть все доступные варианты:

```bash
distrobox --help
```

---

## Вход в контейнер

После извлечения изображения войдите в контейнер:

```bash
distrobox enter debian
```

В Debian использование обычное:

```bash
sudo apt update
sudo apt upgrade
sudo apt autoremove
sudo apt install firefox
```

Вы буквально находитесь внутри другого дистрибутива.

---

## Выполнение команд без входа в контейнер

Вы также можете запускать команды непосредственно с хоста.

Пример: установка Firefox на Debian без его изучения:

```bash
distrobox enter debian -- sudo apt install -y firefox-esr-l10n-pt-br
```

Практично, быстро и традиционно.

---

## Экспорт приложений в хост-систему

Distrobox позволяет экспортировать приложения из контейнера
в графическое меню VoidLinuxBR.

Пример: экспортируйте Firefox из контейнера Debian:

```bash
distrobox enter debian -- distrobox-export --app firefox
```

Приложение появится в меню графической среды.
как будто он родной.

---

## Обновление всех контейнеров

Чтобы обновить все контейнеры одновременно,
выполнить без хоста:

```bash
distrobox-upgrade --all -v
```

---

## Перечисление существующих контейнеров

Чтобы просмотреть все созданные контейнеры:

```bash
distrobox list
```

Отображаются имя, статус и использованное изображение.

---

## Остановка контейнера

Если вам просто нужно остановить контейнер:

```bash
distrobox stop debian
```

---

## Удаление контейнера

Чтобы удалить контейнер Distrobox:

```bash
distrobox rm debian
```

Если вы хотите также удалить изображение Podman:

```bash
podman rmi -f [IMAGE ID]
```

---

## Заключительные наблюдения

- Используйте контейнеры для тестирования, а не для загромождения хоста.
- При необходимости измените имена и изображения.
- Всегда обращайтесь к официальной документации:
https://distrobox.it
- Сначала протестируйте на виртуальной машине или лабораторном компьютере.

Distrobox — инструмент для тех, кто любит контроль,
изоляция и ухоженная система.

---

## 📜 Кредиты

Создатель: Робсон Накане <theblizzard1983@hotmail.com>
Сообщество: Void Linux Бразилия <https://github.com/voidlinuxbr>
Дистрибутив: Chili Linux <https://chililinux.com>
Распространение: VoidBR <https://github.com/voidlinuxbr>

---

## ⚖️ Отказ от ответственности (официальное уведомление)

ДАННОЕ ПРОГРАММНОЕ ОБЕСПЕЧЕНИЕ/РУКОВОДСТВО ПРЕДОСТАВЛЯЕТСЯ «КАК ЕСТЬ» БЕЗ АБСОЛЮТНО БЕЗ ГАРАНТИЙ.
ЛЮБОГО РОДА, ЯВНОГО ИЛИ ПОДРАЗУМЕВАЕМОГО, ВКЛЮЧАЯ, НО НЕ ОГРАНИЧИВАЯСЬ,
ГАРАНТИИ ТОВАРНОСТИ ИЛИ ПРИГОДНОСТИ ДЛЯ ОПРЕДЕЛЕННОЙ ЦЕЛИ.

ИСПОЛЬЗОВАНИЕ ЭТОГО ПРОГРАММНОГО ОБЕСПЕЧЕНИЯ ЯВЛЯЕТСЯ ВСЕЙ ОТВЕТСТВЕННОСТЬЮ ПОЛЬЗОВАТЕЛЯ.

НИКОГДА АВТОР ИЛИ УЧАСТНИКИ НЕ НЕСУТ ОТВЕТСТВЕННОСТИ ЗА
ЛЮБОЙ УЩЕРБ, ПОТЕРЯ ДАННЫХ ИЛИ ОТКАЗ СИСТЕМЫ, ВОЗНИКАЮЩИЙ В РЕЗУЛЬТАТЕ ИСПОЛЬЗОВАНИЯ
ЭТОЙ ПРОГРАММЫ.

---

Авторские права (C) 2026 Робсон Накане
