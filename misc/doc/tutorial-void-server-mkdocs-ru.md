# Установка сервера Mkdocs в Void Linux

## 🎯 Цель — загрузить сервер MkDocs, быстрый, простой и ориентированный на проекты генератор статической документации для веб-сайтов. Он превращает простые файлы Markdown в профессиональный веб-сайт документации с полной навигацией. Конфигурация выполняется с помощью одного файла YAML (mkdocs.yml), а содержимое записывается в стандартном Markdown. Он идеально подходит для создания технической документации, руководств пользователя или баз знаний, предлагая встроенный сервер разработки для просмотра в режиме реального времени.

---

## Установите системные зависимости (Python и pipx) через XBPS.

```bash
sudo xbps-install -S python3 python3-pipx
```

## 🏠 Установите пакет mkdocs в виртуальной среде Python.

```bash
pipx install mkdocs
```

## Добавьте новый путь к операционной системе локально или глобально.

## Местный

```bash
pipx ensurepath
```

## Глобальный

```bash
sudo pipx ensurepath --global
```

## Местоположение появится в файле .bashrc пользователя.

```bash
# Created by `pipx` on 2025-11-27 14:07:54
export PATH="$PATH:/home/suporte/.local/bin"
```

## Проверьте новый путь пользователя к операционной системе.

```bash
source ~/.bashrc
```

## Подтвердить установку пакета

```bash
mkdocs --version
```

## Установка темы Material в виртуальной среде Python

```bash
pipx inject mkdocs mkdocs-material
```

## Инъекция установит пакет темы по скрытому пути в доме пользователя.

```bash
/home/suporte/.local/bin/mkdocs
```

## Последовательность использования средства:

## 1. Создайте новый проект

## 🔧 Чтобы начать новый проект документации, перейдите в каталог, в котором вы хотите создать проект, и запустите:

```bash
mkdocs new Void_Artigos
```

## Это создаст новый каталог под названием Void_Artigos с базовой структурой MkDocs.

## 2. Используйте тему материала (необязательно).

## 🧩 Если вы создали новый проект, отредактируйте файл конфигурации mkdocs.yml внутри каталога проекта (Void_Artigos/mkdocs.yml) и добавьте конфигурацию темы Material:

```bash
site_name: Void Artigos
nav:
    - Home: index.md
    - Sobre: about.md

theme:
  name: material # Adicione esta linha para usar o tema Material
```

## 3. Запустите сервер разработки.

## Чтобы просмотреть документацию локально во время ее редактирования, перейдите в каталог проекта и запустите сервер разработки:

```bash
cd void-Artigos
```

```bash
mkdocs serve
```

## Сервер запустится, и вы сможете получить доступ к документации в своем браузере, обычно по адресу http://127.0.0.1:8000.. MkDocs автоматически отслеживает изменения в ваших файлах и перезагружает страницу.

## Для обслуживания внутренней сети укажите IP-адрес и порт Сервера.

```bash
mkdocs serve 192.168.70.100:8000
```

## Доступность из любого браузера во внутренней сети

```bash
http://192.168.70.100:8000
```

## 4. Создайте статическую документацию

## Когда ваша документация будет готова к публикации, создайте статические файлы:

```bash
mkdocs build
```

## При этом будет создан каталог site/, содержащий все файлы HTML, CSS и JavaScript, необходимые для размещения вашей документации на любом веб-сервере. Короче говоря, использование Void Linux не меняет рабочий процесс MkDocs благодаря использованию pipx, который эффективно изолирует приложение.

---

🎯ВОТ ВСЕ, ЛЮДИ!

👉 Контакт: Zerolies@disroot.org
👉 https://t.me/z3r0l135
